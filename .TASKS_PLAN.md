# .TASKS_PLAN.md — قائمة المهام الأولى
## Engineering Management System v3 · مرتبة حسب الأولوية
> تم إنشاؤها أثناء Genesis Protocol — 2026-06-21

---

## أولوية HIGH

- [x] **دمج الكود المكرر** — `modules/<name>/<name>/` نسخ مكررة من `backend/app/<name>/`
  - المواقع: auth, core, contractors
  - الإجراء: ✅ حُذفت المجلدات المكررة (20 ملفاً)، بقيت `modules/<name>/__init__.py` فقط
- [ ] **بناء بيانات شركة Negida** — جلب بيانات من http://www.negidacontracting.com/ar/
  - إنشاء Company Profile في قاعدة البيانات
  - ربط مع نظام Auth
  - إنشاء API endpoints: `/api/companies`
- [ ] **Frontend Deployment** — `npm run build` + نقل dist إلى backend/frontend/dist
- [ ] **GitHub Push** — دفع المشروع + تحديث README.md + إنشاء `.env.example`
- [ ] **Integration Testing** — `pytest backend/tests -v` كامل

## أولوية MEDIUM

- [ ] **EventBus + Connectors** — تفعيل التواصل بين الموديولات
  - events: `project.created` → hr, `payment_certificate.approved` → finance
  - ports: engineering → hr (get employee)
- [ ] **Search & Export** — توسيع ليشمل HR, finance, inventory
- [ ] **إزالة الـ duplicate modules** — `modules/auth/auth/` (48 ملفاً)
- [ ] **إنشاء CRITIQUE.md** — توثيق المشاكل المفتوحة

## أولوية LOW

- [ ] **إصلاح Sidebar** — إضافة ملاحظات الغرف، حلويات الاجتماعات، ميزانية المشروع
- [ ] **Docker Compose** — إنشاء docker-compose.yml للتطوير المحلي
- [ ] **PostgreSQL Migration** — الانتقال من SQLite إلى PostgreSQL
- [ ] **Alembic auto-generation** — تكوين auto-migration
- [ ] **Soft Delete** — إضافة is_deleted حقل لجميع entities
- [ ] **Tests Coverage** — رفع التغطية إلى > 60%

---

## الملاحظات

- 44/44 اختبارات تنجح حالياً
- Auth P0 fixes مكتملة (SECRET_KEY, token blacklist, rate limiting)
- Frontend DataTable fixes مكتملة (MUI X v8 API)
- المهام مأخوذة من HANDOFF.md السابق + تحليل Genesis
